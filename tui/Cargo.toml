[package]
name = "formation-chess-tui"
version = "0.3.0"
edition = "2024"
description = "Terminal interface for Formation Chess (阵棋)"
repository = "https://github.com/LambdaAlpha/formation-chess"
license = "MIT OR Apache-2.0"
keywords = ["board-game", "abstract-game", "chess", "xiangqi", "strategy"]
categories = ["games"]

[lints]
workspace = true

[dependencies]
formation-chess-core = { path = "../core", version = "0.3" }
rand = "0.10"
